[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Generös timeout för PDF-tolkning (kräver betald plan för >10s; annars håll PDF:er små)
[functions."extract-pdf"]
  timeout = 60
